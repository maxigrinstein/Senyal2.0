---
import { Image } from "astro:assets";
import Layout from "@/layouts/Layout.astro";
import ProductsService from "@/services/products.service";
const params = Astro.params as { id: string };
const product = await ProductsService.getProduct(params.id);
if (!product) return Astro.redirect("/404");
---

<Layout title={product.title}>
    <div class="bg-neutral-100 flex flex-col p-10">
        <div>
            <a href="/">
                <button class="mb-2 text-zinc-900 hover:underline"
                >
                    Volver al inicio
                </button>
            </a>
        </div>
        <div class="flex flex-row">
            <img
                src={product.image}
                alt={product.title}
                class="w-1/3 h-auto rounded-lg"
            />
            <div class="px-10">
                <h1 class="text-2xl font-bold text-neutral-950">
                    {product.title}
                </h1>
                <p class="text-lg mt-2 text-black font-">{product.description}</p>
            </div>
        </div>
    </div>
</Layout>
